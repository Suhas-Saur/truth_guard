# TruthGuard AI Environment Configuration

# Optional: Google Gemini API Key
# If omitted, TruthGuard AI automatically uses its built-in heuristic verification engine.
GEMINI_API_KEY=

# Optional: MongoDB Connection URI
# If omitted, TruthGuard AI automatically persists history to a local JSON database (data/history.json).
MONGODB_URI=
